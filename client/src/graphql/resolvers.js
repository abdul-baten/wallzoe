/* import _ from 'lodash'
import { hasUserById } from '../graphql' */
export default {
  /*   Query: {
    me: async () => {
      if (!_.isEmpty(localStorage.getItem('user'))) {
        const me = JSON.parse(localStorage.getItem('user'))

        if (!_.isEmpty(me)) {
          if (await hasUserById(me._id)) {
            return { isSignedIn: true, ...me }
          }
        }
      }
      return null
    }
  } */
}
